Options -Indexes

<IfModule mod_rewrite.c>
  RewriteEngine On

  # Keep direct /public access unchanged
  RewriteRule ^public/ - [L]

  # Internally map all root requests to /public while keeping URL short
  RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
